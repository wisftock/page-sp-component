image: node:22-slim

cache:
  key:
    files:
      - package-lock.json
  paths:
    - node_modules/

pages:
  script:
    - npm ci
    - npm run build
    - rm -rf public
    - mv _site public
  artifacts:
    paths:
      - public
  only:
    - master
