image: node:22-slim

cache:
  key:
    files:
      - package-lock.json
  paths:
    - node_modules/

pages:
  script:
    - npm ci
    - npm run build
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - master
